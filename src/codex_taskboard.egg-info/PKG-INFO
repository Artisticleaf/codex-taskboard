Metadata-Version: 2.4
Name: codex-taskboard
Version: 0.1.0
Summary: Tmux-backed task orchestration and terminal dashboard for Codex agents on Ubuntu
Author: OpenAI Codex + Awei
License: MIT
Keywords: codex,tmux,orchestration,dashboard,agent
Classifier: Development Status :: 3 - Alpha
Classifier: Environment :: Console
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Operating System :: POSIX :: Linux
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Topic :: System :: Monitoring
Classifier: Topic :: Software Development :: Build Tools
Requires-Python: >=3.11
Description-Content-Type: text/markdown
License-File: LICENSE
Dynamic: license-file

# codex-taskboard

`codex-taskboard` 是面向 Codex / 科研 agent 的本地任务编排器：统一托管 shell 任务、外部 PID、子 agent 和 API 提交任务，并在任务结束后把结果安全回流到对应 session。

## 当前状态机

### 自动化模式

- `managed`
  - 只托管任务与回流 backlog，不自动再唤起当前 session。
- `continuous`
  - 自动把同一链路的回流并入当前 session。
  - 生命周期循环为 `planning -> execution -> closeout -> planning`。

### 科研阶段

- `planning`
  - 读取 history / handoff / closeout / proposal / 关键日志。
  - 必要时补读旧文献与 2024 年后的最新顶刊顶会文献。
  - 刷新 proposal，并把首个执行包准备到可分发状态。
- `execution`
  - 在统一上下文中完成 receipt 吸收、代码/数据审计、局部修复、proposal/history 写回、实验包准备与提交。
  - 当前对话能完成的 CPU-only 工作，不要为了 signal 人为拆步，但也不要把“还能再做一点”无限拆成更小的下一步；execution 必须收束到“提交真实实验”或“进入 closeout”之一。
- `closeout`
  - 只有在 execution 已经明确写出“剩余本地动作已不足以改变结论边界、关键风险判断或实验就绪度，因此继续扩展当前 proposal 已无足够信息增益”的分析后才能进入。
  - 必须完成 proposal 总结、history 回写、handoff、binding 确认，再由 taskboard 启动下一轮 planning。

### 公开 signal

agent 面向 taskboard 的公开 signal 只保留：

- `TASKBOARD_SIGNAL=EXECUTION_READY`
- `TASKBOARD_SIGNAL=WAITING_ON_ASYNC`
- `TASKBOARD_SIGNAL=CLOSEOUT_READY`
- `TASKBOARD_SIGNAL=none`

兼容说明：旧 signal 仍会被 parser 尽量归一化，但 taskboard 不再要求 agent 主动输出它们。

## Prompt 设计

- prompt 文案维护在 `prompts/taskboard_runtime_prompt_zh.toml`。
- 可用 `CODEX_TASKBOARD_PROMPT_FILE` 或 `~/.config/codex-taskboard/taskboard_runtime_prompt_zh.toml` 覆盖本机 prompt，而不用改 runtime 代码。
- 所有 continuous prompt 都显式要求：如果没有人工干预，agent 必须严格思考、审查候选路径后自主执行最优决策，并说明理由。
- `protocol-repair` 是唯一保留的纠错支线：当 agent 漏掉固定尾部协议时，taskboard 发送极短修复 prompt，而不是重新灌整段长 prompt。

## 固定尾部协议

回复末尾固定输出：

- `TASKBOARD_SIGNAL=EXECUTION_READY|WAITING_ON_ASYNC|CLOSEOUT_READY|none`
- `TASKBOARD_SELF_CHECK=pass|fail`
- `LIVE_TASK_STATUS=none|submitted|awaiting`

## 常用命令

```bash
cd /home/Awei/codex-taskboard
python3 -m venv .venv
.venv/bin/pip install -e .
.venv/bin/codex-taskboard doctor

.venv/bin/codex-taskboard automation-mode status --session-id <codex_session_id>
.venv/bin/codex-taskboard automation-mode managed --session-id <codex_session_id>
.venv/bin/codex-taskboard automation-mode continuous --session-id <codex_session_id>

.venv/bin/codex-taskboard enter-stage planning --proposal /path/to/PROPOSAL.md --project-history-file /path/to/HISTORY.md
.venv/bin/codex-taskboard enter-stage execution --session-id <codex_session_id>
.venv/bin/codex-taskboard enter-stage closeout --session-id <codex_session_id> --handoff-file /path/to/HANDOFF.md

.venv/bin/codex-taskboard backlog status --session-id <codex_session_id>
.venv/bin/codex-taskboard backlog show --session-id <codex_session_id>
.venv/bin/codex-taskboard backlog clear --session-id <codex_session_id>

.venv/bin/codex-taskboard api-url
.venv/bin/codex-taskboard prompt-preview --scene planning
.venv/bin/codex-taskboard prompt-preview --scene execution
.venv/bin/codex-taskboard prompt-preview --scene closeout
```

## 提交与托管原则

- 真正需要 GPU、remote、长时间等待或独立生命周期时，才用 `codex-taskboard submit`。
- 本地跨回复长任务用 `bind-before-launch` / `attach-pid` 接管。
- 正式实验默认优先使用 tmux 托管。
- 若同一 `codex_session_id + proposal_path + command` 已存在 live task，taskboard 默认阻止重复提交。

## WAITING_ON_ASYNC 与 backlog

- `WAITING_ON_ASYNC` 表示已有 live task 等回流。
- 进入 `WAITING_ON_ASYNC` 后，taskboard 默认按 1 小时节奏提醒对应 agent 回来确认一次，只为确认实验没卡住且仍有日志/结果产出。
- 回流 backlog 可在 dashboard 或 `codex-taskboard backlog` 中查看与清理。
- dashboard 头部会显示当前 `automation-mode` 和 backlog 计数。

## 相关文档

- `prompts/taskboard_agent_workflow_notice_zh.md`
- `prompts/taskboard_runtime_prompt_zh.toml`
- `docs/prompt-scene-matrix.md`
- `docs/AGENT_WORKFLOW_SPEC_ZH.md`
- `docs/USER_MANUAL.md`
